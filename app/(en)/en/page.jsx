import Landing from '@/components/Landing';

// I metadata (titolo, description, canonical, alternate) arrivano dal layout della lingua.
export default function PageEn() {
  return <Landing locale="en" />;
}
